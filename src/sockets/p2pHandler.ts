import { Server ,Socket } from "socket.io";
import type { ChatMessagePayload, ServerMessageBroadcast } from "./types.js";
import prisma from "../prisma.js";

export default (io: Server, socket: Socket) => {
  
};
